# Optional: only for search-engine verification codes.
# The site URL is hardcoded in lib/data.ts (siteConfig.url).
# Copy to .env.local and fill in. The site works without these.

NEXT_PUBLIC_GSC_VERIFICATION=
NEXT_PUBLIC_BING_VERIFICATION=
